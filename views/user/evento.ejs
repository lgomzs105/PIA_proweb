<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>
        <%= evento.titulo %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-dark text-white">

    <div class="container py-5">
        <a href="/calendario" class="btn btn-outline-success mb-4">← Volver al calendario</a>

        <main class="container text-white py-5">
            <h1>
                <%= evento.titulo %>
            </h1>
            <img src="<%= evento.portada %>" class="img-fluid mb-4" />

            <ul class="list-unstyled">
                <li><strong>📍 Lugar:</strong>
                    <%= evento.lugar %>
                </li>
                <li><strong>📅 Duración:</strong>
                    <%= evento.duracion %>
                </li>
                <li><strong>🕒 Horario:</strong>
                    <%= evento.horario %>
                </li>
            </ul>

            <div class="mt-4">
                <% evento.descripcion.forEach(p=> { %>
                    <p>
                        <%= p %>
                    </p>
                    <% }) %>
            </div>
        </main>

    </div>

</body>

</html>