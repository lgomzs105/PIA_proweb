<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>
        <%= evento.titulo %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-dark text-white">

    <div class="container py-5">
        <a href="/calendario" class="btn btn-outline-success mb-4">← Volver al calendario</a>

        <h1 class="mb-3 text-success fw-bold">
            <%= evento.titulo %>
        </h1>

        <img src="<%= evento.portada %>" alt="Portada" class="img-fluid rounded mb-4" />

        <ul class="list-group list-group-flush mb-4">
            <li class="list-group-item bg-dark text-white">📅 <strong>Fecha:</strong>
                <%= evento.fecha %>
            </li>
            <li class="list-group-item bg-dark text-white">📍 <strong>Lugar:</strong>
                <%= evento.lugar %>
            </li>
            <li class="list-group-item bg-dark text-white">🕒 <strong>Horario:</strong>
                <%= evento.horario %>
            </li>
            <li class="list-group-item bg-dark text-white">📆 <strong>Duración:</strong>
                <%= evento.duracion %>
            </li>
        </ul>

        <div>
            <% evento.descripcion.forEach(p=> { %>
                <p>
                    <%= p %>
                </p>
                <% }) %>
        </div>
    </div>

</body>

</html>