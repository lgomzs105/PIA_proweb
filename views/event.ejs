<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
        <%= evento.titulo %>
    </title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/styles/main.css" />
</head>

<body class="bg-dark text-white">
    <div class="container py-5">

        <div class="text-center mb-4">
            <img src="<%= evento.portada %>" class="img-fluid rounded shadow" alt="Portada <%= evento.slug %>">
        </div>

        <div class="d-flex align-items-center gap-3 mb-4">
            <img src="<%= evento.icono %>" alt="Icono" style="width: 32px; height: 32px;">
            <h1 class="text-success fw-bold m-0">
                <%= evento.titulo %>
            </h1>
        </div>

        <table class="table table-dark table-striped table-bordered mb-4">
            <tbody>
                <tr>
                    <th scope="row">📅 Fecha</th>
                    <td>
                        <%= evento.fecha %>
                    </td>
                </tr>
                <tr>
                    <th scope="row">📍 Lugar</th>
                    <td>
                        <%= evento.lugar %>
                    </td>
                </tr>
                <tr>
                    <th scope="row">📆 Duración</th>
                    <td>
                        <%= evento.duracion %>
                    </td>
                </tr>
                <tr>
                    <th scope="row">🕘 Horario</th>
                    <td>
                        <%= evento.horario %>
                    </td>
                </tr>
            </tbody>
        </table>

        <div class="mb-4">
            <% evento.descripcion.forEach(linea=> { %>
                <p>
                    <%= linea %>
                </p>
                <% }) %>
        </div>

        <a href="/calendario" class="btn btn-outline-light">← Volver al calendario</a>
    </div>
</body>

</html>